<% if signed_in? %>
  <div class="row">
	<aside class="span4">
	  <section>
		<%= render 'shared/user_info'%>
	  </section>
	</aside>
  </div>

<% else %>
<div class="center hero-unit">
  <h1>Welcome to Picture Share</h1>
  <h2>Share your pictures and photos in here!</h2>
  <%= link_to "Sign up now!", new_user_registration_path,class: "btn btn-large btn-primary" %>
</div>
<% end %>
<%= link_to image_tag("home.jpg", alt: "PicShare"), 'http://blog.maxwi.com' %>
