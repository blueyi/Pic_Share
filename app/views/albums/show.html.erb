<% provide(:title, 'Album') %>

<p id="notice"><%= notice %></p>
<p>
  <strong>Album title:</strong>
  <%= @album.title %>
<p>

<%= will_paginate @pics %>
<ul>
<%= render @album_pics %>
</ul>
<%= will_paginate @pics %>

<br>
<p>
  <%= @album.content %>
</p>

<%= link_to 'Back', current_user %>

